import { CodeInsertionDirective } from './code-insertion.directive';

describe('CodeInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
